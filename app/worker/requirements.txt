# Cliente RabbitMQ/AMQP usado por el worker.
pika>=1.3,<2
# Driver PostgreSQL moderno con wheel binaria para evitar compilar en Docker.
psycopg[binary]>=3.1,<4
# Cliente AWS para enviar mensajes a SQS DLQ.
boto3>=1.34,<2
# Permite ejecutar localmente leyendo .env, aunque en ECS usamos env vars.
python-dotenv>=1.0,<2
